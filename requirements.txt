# Core ML Framework
torch>=2.0.0
numpy>=1.24.0

# Graph & Network Analysis
networkx>=3.0

# Data Processing
pandas>=2.0.0
scikit-learn>=1.2.0
pyarrow
pycountry

# Data Sources
fredapi
requests
owid-catalog

# Visualization (for figure generation)
matplotlib>=3.7.0

# Statistical Modeling (for PPML baseline)
statsmodels>=0.14.0

# Document Generation (optional, for manuscript output)
python-docx>=1.0

# Note: torch_geometric is listed in some imports but is NOT required.
# All GNN operations use pure PyTorch scatter ops to avoid C++ kernel
# crashes on Windows. Install only if you need PyG-specific utilities:
# torch_geometric>=2.3.0
